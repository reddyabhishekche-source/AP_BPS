# AP_BPS

## Architecture

See `docs/architecture.md`.

## Local dev prerequisites

- Start dependencies (Redis + Postgres): `docker compose up -d`
- Ensure `.env` has `REDIS_URL=redis://localhost:6379`
